candidate_name: Liam Van

Put your generic source resume files in this folder.

- `candidate_name` is used by the resume-tailor skill to name company output folders
- `candidate_name` is also used to name rendered PDFs
- Example output folder: `companies/Microsoft/Liam_Van_Resume`
- If that folder already exists, the skill should create `Liam_Van_Resume_2`, then `_3`, and so on
