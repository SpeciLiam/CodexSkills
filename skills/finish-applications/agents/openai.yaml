interface:
  display_name: "Finish Applications"
  short_description: "Complete ready applications in one Chrome-driven agent"
  default_prompt: "Use $finish-applications to work through my ready but unapplied job applications in the current agent. Use Chrome directly for live application flows, ask me only when blocked, update the tracker/cache yourself, and use a concise file-based handoff only if context gets overloaded."
candidate_profile:
  gender: "male"
  transgender: false
  standing_submit_intent: "Attempt and complete routine applications for rows with tailored resumes; only stop for true blockers."
browser_form_handling:
  filtered_selects: "Open/filter the dropdown and select the real option; do not leave typeahead text unselected."
