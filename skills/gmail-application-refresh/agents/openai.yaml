interface:
  display_name: "Gmail Application Refresh"
  short_description: "Update application statuses from Gmail"
  default_prompt: "Use $gmail-application-refresh to scan Gmail for application updates and safely update the markdown tracker."
