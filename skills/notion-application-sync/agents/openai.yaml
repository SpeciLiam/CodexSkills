interface:
  display_name: "Notion Application Sync"
  short_description: "Mirror application tracker data to Notion"
  default_prompt: "Use $notion-application-sync to preview or sync the generated application tracker data into Notion."
